# Write value to stdout without newline
fn write(x)
    emit(str(x))


# Write value to stdout followed by newline
fn print(x)
    write(x)
    emit("\n")


# Print error message and halt execution
fn error(msg)
    print("ERROR: " . msg)
    1 / 0  # Halt execution via division by zero
