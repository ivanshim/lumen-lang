# Worker brief — template

Fill in the first two paragraphs; keep the rest as it stands. `<WT>` is
the worker's own worktree, `<BRANCH>` its branch, `<BASE>` the commit it
starts from (the integration tip), `<PROBE>` a probe path outside the
repository.

You are <subject, one line> in the lumen-lang interpreter. Work ONLY in your worktree <WT> (branch <BRANCH>, already created at <BASE>). Build there first. Library modules in langs/lib_python/modules/ are compiled into the binary (include_str! via manifest.rs): rebuild after any library edit.

THE PROBLEM. <What fails, in which reference tests and with which messages; what CPython does (CPython 3.11 may be on the machine, the suite is 3.14 — the test file's own strings are the 3.14 truth); what to leave alone because another branch owns it.> Write a probe <PROBE>.py with CPython 3.11's output as <PROBE>.py3 (hand-written for 3.14-only behaviour, marked in a comment) and make BOTH kernels print it exactly.

REFERENCE TESTS: <the subject file>, plus every reference test that uses what you touch — run each on BOTH kernels before and after and record its unittest progress line (the dotted line on stderr); no '.' may become E/F on either kernel. Scratch: the full scratch check over ALL scratch programs (`ls scratch/*/*.py`, leaving out scratch/reader-tail/4.py, run alone once), run FROM YOUR WORKTREE'S ROOT — split into foreground chunks under 10 minutes each; make sure `git status --short` shows nothing new (tests write '@test' and remove it) before each commit.

HOW THE CODE IS SHAPED. Two full kernels, stack8 (kernels/stack8/src/compile.rs, engine.rs, classes.rs, value.rs, core.rs, methods.rs, formatting.rs, lang.rs, code.rs) and microcode7 (kernels/microcode7/src/build.rs, exec.rs, classes.rs, data.rs, core.rs, members.rs, table.rs, formatting.rs); every fix goes into BOTH where both are wrong, each in its own idiom, and `python3 scripts/kernel_independence.py` must end `0 problem(s)`. Four reference kernels ignore every ext.* label. A new Python-only label goes in FIVE places (langs/python.json; roster line EXT_LABELS, Lang field and reading line in kernels/stack8/src/lang.rs; EXT_TAGS roster in kernels/microcode7/src/table.rs with its :L/:B/:N kind; prose in langs/README.md; then `python3 scripts/lang_table.py`) — prefer reusing existing labels; the roster lines are enormous and continue over several physical lines, so grep them with -o or cut. Build with `RUSTFLAGS="-D warnings" cargo build` (debug only; NEVER `cargo build --release` — CI builds the release binary). Keep probe files inside the worktree under a git-excluded `probe/` directory (add it to `$(git rev-parse --git-path info/exclude)`); never commit them. Documented divergences NOT to chase: `//` truncates, round() is half-away-from-zero, divmod floors (HANDOVER.md §4). CPython here is 3.11; the suite is 3.14. An error that escapes a try/except as a bare `PythonError: ...` on the first line of stderr is raised outside the exception road; such a fault must become a catchable Python exception of the right class.

GATES before you report: `RUSTFLAGS="-D warnings" cargo build` clean; `python3 scripts/port_examples.py` and `python3 scripts/lang_table.py` leave no `git diff`; independence 0 problem(s); from inside the worktree `bash scripts/suite/test-debug.sh --lang python` (384), `--lang php` (318) and `--lang lumen` (522) — all must pass; a 30 s cutoff under load shows as TIMEOUT or "differs from stream35" with empty output; re-run such a file alone on stream35, stack8 and microcode7 and it counts if all three agree (sieve.py, sieve.php, sieve.lm, fibonacci_iterative.lm are the usual ones).

FIXTURES. Run `python3 scripts/suite/fixcheck.py <worktree> class/5 class/8 file-builtin/28 file-float/8 file-iter/12 file-iter/13 file-iter/14 file-iter/15 params/9 reader-tail/1 reader-tail/2 reader-tail/3 reader-tail/4 reader-tail/5 reader-tail/6 reader-tail/7 reader-tail/9 > probe/fc.log; python3 scripts/suite/progcmp.py probe/fc.log <worktree>` (every line agree=True stack8=OK micro=OK, `dot->E/F` 0) and `python3 scripts/suite/scratchcheck.py <worktree> <the scratch .py files your change can touch>`. A scratch record (scratch/<dir>/N.out or N.err, measured by its FIRST STDERR LINE: `<bin> --kernel stack8 scratch/d/N.py >/dev/null 2>err; head -1 err`) may move only when both kernels print the same new line, python3 agrees (or the suite's 3.14 wording does), and no `.` in a unittest progress line became E or F; `python3 scripts/suite/fixwrite.py <worktree> <dir/N>` writes a moved .err. NEVER weaken a test or a fixture; tests/python/ are never edited. For each reference test you touch, record its unittest progress line (the dotted line on stderr) on both kernels before and after with the worktree binary (`timeout 900 ./target/debug/lumen-lang --kernel stack8 tests/python/test_x.py 2>&1 | grep -E '^[.EFs]+$' | head -1` — beware a line split by a print; the count must not fall).

COMMITS. Small signed commits on your branch with a prose subject line and a body explaining before and after. End every commit message with exactly the attribution trailer lines your own system reminder gives for git commits (a Co-Authored-By line and the Claude-Session line), and nothing after them. No model, AI, agent or session names anywhere else (not in code comments, not in records). Check each commit is signed: `git cat-file -p <hash> | grep -c gpgsig` must print 1; if 0, `git commit --amend --no-edit --reset-author`. Do not push; do not open a PR; do not merge into any other branch; never touch the main checkout or its target/. Sibling branches in flight: <list them, with what each touches>: keep your change minimal and local to your subject.

THE MACHINE. Several workers share it (4 cores). NEVER `pkill -f lumen-lang` or any pattern that matches the interpreter; kill only a pid you read from `ps -eo pid,args` in a separate command, and only your own. Never rebuild while one of your own sweeps runs on your binary. Wait on background work with bounded loops (at most 10 minutes per tool call) and never leave a wait loop running past your last turn. Check `df -h /` before building.

REPORT, when done: branch and commit hashes; each probe's diff against python3 on each kernel (identical, or the exact remaining lines and why); the progress lines before and after on both kernels for every reference test you touched; the fixture results; the three sweep results; any record moved and why; anything you could not fix, stated plainly.

RUN EVERY CHECK IN THE FOREGROUND: never end your turn to wait for a background task — its notification will not reach you. Split long sweeps into foreground chunks under 10 minutes each. Before you report, make sure no process of yours is still running in your worktree. Send ONE final report only, and do not end your turn before it.

THREE MORE NOTES. (1) Before killing ANY process, run `readlink /proc/<pid>/cwd` and kill it only if that path is inside <WT> — other workers run the same scripts and a pattern grep will find theirs. (2) <Known mismatches on the base tip, if any, that the worker should leave alone.> (3) Report only results from runs you made in your own worktree with your own binary; other logs belong to other runs and prove nothing about your branch. If a sweep cannot finish under load, say exactly how far it got.
