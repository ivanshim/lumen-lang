LET scale = 1000000;
LET sum = scale;
LET term = scale;
LET n = 1;
LET iterations = 0;
LET max_iter = 100;

while (iterations < max_iter) {
    LET term = term / n;
    LET sum = sum + term;
    LET n = n + 1;
    LET iterations = iterations + 1;
}

PRINT(sum);
