[package]
name = "lumen-stack8"
version = "0.1.0"
edition = "2021"
description = "Stack kernel, third design: eight words, three of them fused for speed"

[dependencies]
num-bigint = "0.4"
num-traits = "0.2"
num-integer = "0.1"
serde_json = "1"
lumen-room = { path = "../room" }

[lib]
path = "src/lib.rs"
