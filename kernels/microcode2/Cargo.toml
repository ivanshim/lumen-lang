[package]
name = "lumen-microcode2"
version = "0.2.0"
edition = "2021"
description = "Microcode kernel, second design: a tree of primitives that runs, and writes itself back out in any language"

[lib]
path = "src/lib.rs"

[dependencies]
num-bigint = "0.4"
num-traits = "0.2"
num-integer = "0.1"
serde_json = "1"
