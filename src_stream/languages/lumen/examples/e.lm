SCALE = 10000000000

sum = SCALE
term = SCALE
n = 1

while term > 0
    term = term / n
    sum = sum + term
    n = n + 1

int_part = sum / SCALE
frac_part = sum % SCALE

print(int_part)
print(".")
print(frac_part)
