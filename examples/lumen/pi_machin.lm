# Parameter: significant_figures (default: 200 here; bench/pi.lm computes 1000)
# Usage: lumen-lang pi_machin.lm [significant_figures]
# Example: lumen-lang pi_machin.lm 200
# Default: Computes 200 digits of pi using Machin's formula

# import("lib_lumen/pi_machin.lm")  # Not needed - functions already linked from lib_lumen

significant_figures = 200
# Note: ARGS parsing with to_int() doesn't work yet - to_int() doesn't parse strings
# if ARGS != ""
#     significant_figures = to_int(ARGS)

result = pi_machin(significant_figures)
result_string = to_string(result)
print(substring_start(result_string, 100))
