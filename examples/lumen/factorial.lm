# Parameter: n (default: 50)
# Usage: lumen-lang factorial.lm [n]
# Example: lumen-lang factorial.lm 50
# Limitation: Microcode kernel stack overflow limits n to ~60
n = 50
if ARGS != ""
    n = int(ARGS)

write("Factorial of ")
write(n)
write(": ")
print(factorial(n))
