# ===== PARAMETER: Edit this value to test factorial with different numbers =====
n = 1000
# ==============================================================================

fn factorial(x)
    if x <= 1
        1
    else
        x * factorial(x - 1)

write("Factorial of ")
write(n)
write(": ")
print(factorial(n))
