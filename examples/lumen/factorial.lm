# Parameter: n (default: 1000)
# Usage: lumen-lang factorial.lm [n]
# Example: lumen-lang factorial.lm 50
n = 1000
if ARGS != ""
    n = int(ARGS)

fn factorial(x)
    if x <= 1
        1
    else
        x * factorial(x - 1)

write("Factorial of ")
write(n)
write(": ")
print(factorial(n))
