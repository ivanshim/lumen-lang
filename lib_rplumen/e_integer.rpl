# The Lumen library file lib_lumen/e_integer.lm, ported by scripts/port_examples.py; edit the Lumen original, not this file.

« 'sigfigs' =
    3 'guard' =
    sigfigs guard + 'work' =
    sigfigs 1 - 'decimals' =
    1 'scale' =
    0 'i' =
    while i work <
        scale 10 * 'scale' =
        i 1 + 'i' =
    scale 'sum' =
    1 'fact' =
    1 'n' =
    work 50 + 'max_terms' =
    while n max_terms <
        scale fact / 'term' =
        term 0 == if
            break
        sum term + 'sum' =
        n 1 + 'n' =
        fact n * 'fact' =
    sum scale / 'result' =
    result decimals round 'rounded' =
    rounded sigfigs real return
» 'e_integer' =
